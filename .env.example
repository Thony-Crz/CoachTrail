# Environment Variables for CoachTrail

# CORS Proxy URL (required for production deployment on GitHub Pages)
# Leave empty for development (Vite proxy will be used automatically)
# 
# For production, deploy the Cloudflare Worker (see proxy/cloudflare-worker.js)
# and set this to your worker URL (e.g., https://your-worker.your-subdomain.workers.dev)
VITE_POLAR_PROXY_URL=
