# Environment Variables for CoachTrail

# Backend URL (optional for production if using same domain)
# Leave empty for development (Vite proxy will be used automatically)
# For production with separate backend, set to your Vercel deployment URL
# Example: https://coach-trail.vercel.app
VITE_BACKEND_URL=

# DEPRECATED: CORS Proxy URL (no longer needed with Vercel backend)
# This was used for Cloudflare Workers proxy approach
# VITE_POLAR_PROXY_URL=
